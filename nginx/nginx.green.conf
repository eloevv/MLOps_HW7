events {
    worker_connections 1024;
}

http {
    upstream ml_backend {
        server ml-service-green:8000;
    }

    server {
        listen 80;

        location /health {
            proxy_pass http://ml_backend;
            proxy_set_header Host $host;
        }

        location /predict {
            proxy_pass http://ml_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
